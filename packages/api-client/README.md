# Shared API Client (Generated)

This package is generated from the backend `openapi.yaml` and shared across all frontends.

## Generate + Build
- `npm run generate`
- `npm run build`

The generated sources live in `src/` and compiled output in `dist/`.

