# Brand logos for carousel

Add logo images here for the brand carousel (above Shop by Category).

- **Filename:** `{slug}.png` where slug is the brand name lowercased and hyphenated (e.g. `samsung.png`, `apple.png`).
- **Size:** 50×100px recommended (portrait); images are shown with `object-fit: contain`.
- If a logo is missing, the first letter of the brand name is shown as fallback.
